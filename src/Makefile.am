INCLUDES = $(GLIB_CFLAGS) $(GTK_CFLAGS) \
    -DSHARE_DIR=\"$(pkgdatadir)\"

bin_PROGRAMS = gdman

gdman_LDADD = $(GLIB_LIBS) $(GTK_LIBS)
gdman_SOURCES = \
    shell.c shell.h

EXTRA_DIST=
CLEANFILES=
